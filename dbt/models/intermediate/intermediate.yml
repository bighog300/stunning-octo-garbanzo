version: 2

models:
  - name: int_artwork_normalized
    columns:
      - name: raw_artwork_id
        tests:
          - not_null

  - name: int_gallery_normalized
    columns:
      - name: raw_gallery_id
        tests:
          - not_null
          - unique
